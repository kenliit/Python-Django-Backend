from django.urls import path

urlpatterns = [
    # url(r'^checkConnection/$', check_connection, name='checkConnection'),
    # path('single_image_or_video/', ImageOrVideoSingleView.as_view(), name='single_image_or_video'),
    # path('upload_image_or_video/', ImageOrVideoSingleView.as_view(), name='upload_image_or_video'),
]



